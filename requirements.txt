# Core dependencies
aiohttp>=3.9.0
websockets>=12.0

# Web UI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Image processing (optional but recommended)
Pillow>=10.0.0

# OCR - choose one (optional)
# easyocr>=1.7.0  # Recommended, but larger download
# pytesseract>=0.3.10  # Requires tesseract-ocr system package

# For development
numpy>=1.24.0  # Required for easyocr

# Optional: for better async support
uvloop>=0.19.0; sys_platform != 'win32'
